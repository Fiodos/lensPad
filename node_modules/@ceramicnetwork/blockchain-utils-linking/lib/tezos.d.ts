import { AccountID } from 'caip';
import { AuthProvider } from './auth-provider';
import { LinkProof } from './util';
import type { Signer } from '@taquito/taquito';
export declare const TEZOS_NAMESPACE = "tezos";
export declare const TEZOS_CHAIN_REF = "NetXdQprcVkpaWU";
export interface TezosProvider {
    signer: Signer;
}
export declare class TezosAuthProvider implements AuthProvider {
    private readonly provider;
    readonly isAuthProvider = true;
    constructor(provider: TezosProvider);
    authenticate(message: string): Promise<string>;
    createLink(did: string): Promise<LinkProof>;
    accountId(): Promise<AccountID>;
    withAddress(_address: string): AuthProvider;
}
//# sourceMappingURL=tezos.d.ts.map