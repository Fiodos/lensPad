import type { DefinitionName, PublishedSchemas } from '@ceramicstudio/idx-constants';
import type { DID } from 'dids';
import type { Definition, SignedDefinitions } from './types';
export declare function createIDXDefinitions(schemas: PublishedSchemas): Record<DefinitionName, Definition>;
export declare function createIDXSignedDefinitions(did: DID, schemas: PublishedSchemas): Promise<SignedDefinitions>;
