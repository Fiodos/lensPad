'use strict';

var codec = require('./codec.js');

var json = codec.codec({
  name: 'json',
  code: 512,
  encode: json => new TextEncoder().encode(JSON.stringify(json)),
  decode: bytes => JSON.parse(new TextDecoder().decode(bytes))
});

module.exports = json;
