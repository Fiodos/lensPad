'use strict';

var bytes = require('../bytes.js');
var codec = require('./codec.js');

const raw = bytes$1 => bytes.coerce(bytes$1);
var raw$1 = codec.codec({
  name: 'raw',
  code: 85,
  decode: raw,
  encode: raw
});

module.exports = raw$1;
