'use strict';

var hasher = require('./hasher.js');
var bytes = require('../bytes.js');

var identity = hasher.from({
  name: 'identity',
  code: 0,
  encode: input => bytes.coerce(input)
});

module.exports = identity;
